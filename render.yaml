services:
  - type: web
    name: miapi
    env: dotnet
    branch: main
    buildCommand: dotnet nuget locals all --clear && dotnet restore MiApi.csproj && dotnet publish MiApi.csproj -c Release -o out
    startCommand: dotnet out/MiApi.dll
